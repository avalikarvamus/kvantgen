# kvantgen

Current default Python interpreter is Pypy.

Make Virtualenv.

Install dependencies:

pip install -r requirements.txt

Make database:

pypy db_create.py

Fill database:

pypy db_filldata.py

Run:

pypy main.py
